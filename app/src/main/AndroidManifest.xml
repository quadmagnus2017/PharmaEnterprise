<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.quadmagnus.pharma">

    <uses-permission android:name="android.permission.INTERNET" />

    <!-- Samsung -->
    <uses-permission android:name="com.sec.android.provider.badge.permission.READ" />
    <uses-permission android:name="com.sec.android.provider.badge.permission.WRITE" />

    <!-- Sony -->
    <uses-permission android:name="com.sonyericsson.home.permission.BROADCAST_BADGE" />

    <!-- HTC -->
    <uses-permission android:name="com.htc.launcher.permission.READ_SETTINGS" />
    <uses-permission android:name="com.htc.launcher.permission.UPDATE_SHORTCUT" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".AppIntroActivity"
            android:screenOrientation="portrait"
            android:theme="@style/MyMaterialTheme"
            android:windowSoftInputMode="stateHidden">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>


        <!-- SignUp Activity -->
        <activity
            android:name=".NewSignUpActivity"
            android:screenOrientation="portrait"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar.FullScreen" />

    <activity android:name=".SignupActivity"
              android:theme="@style/MyMaterialTheme"/>

        <!-- Registration Activity -->
        <activity
            android:name=".RegisterActivity"
            android:screenOrientation="portrait" />

        <!-- LoginActivity  Activity -->
        <activity
            android:name=".LoginActivity"
            android:screenOrientation="portrait"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar.FullScreen" />

        <!-- SelectTypeActivity -->
        <activity
            android:name=".SelectTypeActivity"
            android:screenOrientation="portrait"
            android:theme="@style/MyMaterialTheme" />


        <!-- NewMainActivity -->
        <activity
            android:name=".NewMainActivity"
            android:screenOrientation="portrait"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar"/>


        <!--SearchActivity-->
        <activity
            android:name=".SearchActivity"
            android:screenOrientation="portrait"
            android:theme="@style/MyMaterialTheme"/>

        <!--PrivatePolicyActivity-->
        <activity
            android:name=".PrivatePolicyActivity"
            android:screenOrientation="portrait" />

        <!--TermsConditionAcitivity-->
        <activity
            android:name=".TermsConditionAcitivity"
            android:screenOrientation="portrait" />

        <!--RegisterDoctorActivity-->
        <activity
            android:name=".RegisterDoctorActivity"
            android:screenOrientation="portrait" />

        <!--RegisterInstituteActivity-->
        <activity
            android:name=".RegisterInstituteActivity"
            android:screenOrientation="portrait" />


        <activity android:name=".NewOrderActivity"
            android:theme="@style/MyMaterialTheme"/>

        <activity android:name=".MyOrderActivity"
            android:theme="@style/MyMaterialTheme"/>

        <activity android:name=".DraftOrderActivity"
            android:theme="@style/MyMaterialTheme"/>

        <activity android:name=".SchemeActivity"
            android:theme="@style/MyMaterialTheme"/>


        <meta-data
            android:name="io.fabric.ApiKey"
            android:value="47f6a875addbd12caafc9c78ac5fbfca31e8965f" />
    </application>

</manifest>